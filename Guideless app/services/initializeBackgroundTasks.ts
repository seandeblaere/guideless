// This file MUST be imported at the app's entry point
// It registers the background task definitions

// Import the GeofencingService to register the TaskManager.defineTask
import './GeofencingService';

export {}; 